export type NumberBlocksProps = {
    active: boolean;
};

export type NumberBlockProps = {
    values: number[];
    index: number;
    totalBlocks: number;
};

export type NumberBlocksData = number[][];